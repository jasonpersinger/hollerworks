# Startup Greenhouse Sweep

## Best new lanes from this pass

- `Skild AI`: promoted startup source with public salary ranges and a clean approved AI/robotics/software slice
- `Astrobotic`: promoted Pittsburgh PrimePay Recruit source with a clean approved `3`-role engineering slice and public salary ranges
- `BlastPoint`: real Pittsburgh Greenhouse board, public compensation visible, and one clean `DevOps Manager` role already passes the current filter
- `Gather AI`: real Pittsburgh Greenhouse board with a strong technical mix, but the current public technical postings do not show usable compensation
- `Hellbender`: real Pittsburgh Greenhouse board, but current public postings do not show usable compensation
- `Stack AV`: real Pittsburgh-heavy Greenhouse board, but current public technical postings do not show usable compensation

## What we learned

This was a good discovery pass because it separated a real near-term source from a strong-but-blocked one:

- `Skild AI` is no longer just a candidate; it cleared review cleanly and is now in regular rotation
- `Astrobotic` cleared review cleanly and confirms that PrimePay Recruit is worth supporting
- `BlastPoint` is worth a real review pass now
- `Gather AI` is worth keeping in the queue, but not worth weakening the compensation standard for
- `Hellbender` and `Stack AV` are both worth remembering, but both stay blocked until compensation is public
- `Bellhop` still looks stale in public search results, so it stays demoted until a real live board reappears
- `ERIN` showed up in search results, but the public board path did not resolve cleanly enough to treat it as a live source

## Practical takeaway

The startup lane is getting better, not just bigger. We now have:

- one more startup source that has graduated into regular rotation
- one more startup source that has graduated into regular rotation on a newly supported ATS
- one more startup source that is clearly in-bounds but honestly blocked

That is exactly the kind of progress we want at this stage.
